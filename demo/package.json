{
    "dependencies": {
        "flappa-doormal": "latest"
    },
    "description": "Interactive demo for the flappa-doormal Arabic text segmentation library",
    "devDependencies": {
        "typescript": "~6.0.3",
        "vite": "^8.0.10"
    },
    "name": "flappa-doormal-demo",
    "private": true,
    "scripts": {
        "build": "tsc && vite build && cp CNAME dist/",
        "deploy": "bun run build && surge ./dist --domain flappa-doormal.surge.sh",
        "dev": "vite",
        "preview": "vite preview"
    },
    "type": "module",
    "version": "1.0.0"
}
